// Check if a number is positive or negative
#include <stdio.h>

int main() {
    int number = -3;
    if (number > 0) {
        printf("The number is positive.\n");
    } else {
        printf("The number is negative.\n");
    }
    return 0;
}
