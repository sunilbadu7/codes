// Testing the string
#include<stdio.h>
#include<conio.h>
int main()
{
	char a[10];
	printf("Enter any word ");
	scanf("%s",a);//String  ma address halna pardena because a itself acts as a address
	printf("\n The entered word is %s",a);
	return 0;
}
